/* Loop */

var: integer;

main: function void () = {
    newvar: float = 3.14;
    if (newvar + 1.1)
        newvar++;
    else
        newvar--;

    if (var && var)
        var++;
    else
        var--;
}
