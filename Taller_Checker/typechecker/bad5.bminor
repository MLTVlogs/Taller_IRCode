/* Conditions */

var: integer;

main: function void () = {
    if (var == 0)
        var++;
    else
        var--;

    newvar: integer = 0;
    if (newvar)
        newvar++;
    else
        newvar--;
}
